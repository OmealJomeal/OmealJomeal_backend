<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.config.ProductMapper">

	<insert id="insert" parameterType="ProductDTO">
		<![CDATA[
		insert into product ( product_name, product_price, product_description, product_category, product_img, product_clear_img )
		values (  #{product_name}, #{product_price}, #{product_description}, #{product_category}, #{product_img}, #{product_clear_img} )
		]]>
	</insert>

<!--	<select id="search" resultType="ProductDTO" parameterType="hashmap">-->
<!--		<![CDATA[-->
<!--		select id,-->
<!--			   member_name as memberName,-->
<!--			   title,-->
<!--			   datetime,-->
<!--			   replies,-->
<!--			   likes,-->
<!--			   views-->
<!--		from post-->
<!--		where (${searchCol} like '%'||#{keyword}||'%') AND (category = #{category})-->
<!--		order by id desc-->
<!--		OFFSET (#{pageDto.currentPage}-1)*#{pageDto.limit} ROWS FETCH FIRST #{pageDto.limit} ROWS ONLY-->
<!--		]]>-->
<!--	</select>-->


</mapper>